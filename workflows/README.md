# Workflows

Markdown SOPs that define what to do and how. Each workflow covers: **objective** (what success looks like), **inputs** (what's needed to start), **steps** (which tools to call and in what order), **outputs** (where results end up), and **edge cases** (known failure modes and how to handle them). Keep workflows updated as you learn — they are living instructions, not one-time notes.
